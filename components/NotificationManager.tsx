// This component has been intentionally cleared to remove all notification-related functionality
// as per the user's request, to prevent conflicts within the Telegram Web App environment.
import React from 'react';

const NotificationManager: React.FC = () => {
    return null; // The component renders nothing and contains no logic.
};

export default NotificationManager;